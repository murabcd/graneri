{
  "name": "graneri",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.10",
  "scripts": {
    "build": "turbo run build",
    "build:vercel": "node scripts/build-vercel.mjs",
    "changeset": "changeset",
    "release:prepare": "bun install --frozen-lockfile && bun run version-packages && bun run check && bun run typecheck && bun run test",
    "version-packages": "changeset version",
    "dev": "concurrently --kill-others-on-fail -n web,desktop,convex -c green,cyan,magenta \"bun run dev:web\" \"bun run dev:desktop\" \"bunx convex dev\"",
    "dev:marketing": "turbo run dev --filter=marketing",
    "dev:web": "GRANERI_ENV_MODE=local turbo run dev --filter=web",
    "dev:desktop": "GRANERI_ENV_MODE=local bun --filter=desktop run dev",
    "dev:desktop:native": "GRANERI_ENV_MODE=local bun --filter=desktop run dev:bundled",
    "dist:mac": "GRANERI_ENV_MODE=production bun --filter=web run build:desktop && GRANERI_ENV_MODE=production bun --filter=desktop run build && GRANERI_ENV_MODE=production bun --filter=desktop run package:mac",
    "release:mac": "GRANERI_ENV_MODE=production bun --filter=web run build:desktop && GRANERI_ENV_MODE=production bun --filter=desktop run build && GRANERI_ENV_MODE=production bun --filter=desktop run package:mac:release",
    "doctor:self-host": "node scripts/check-self-host-env.mjs",
    "lint": "turbo run lint",
    "check": "turbo run check",
    "format": "turbo run format",
    "prepare": "husky",
    "test": "turbo run test && bun run test:convex",
    "test:convex": "bun run --cwd convex test:once",
    "typecheck": "turbo run typecheck && bun run typecheck:convex",
    "typecheck:convex": "bun run --cwd convex typecheck",
    "preview": "turbo run preview --filter=web",
    "preview:marketing": "turbo run preview --filter=marketing"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.3",
    "@changesets/cli": "^2.30.0",
    "@edge-runtime/vm": "^5.0.0",
    "@types/node": "^24.12.0",
    "concurrently": "^9.2.1",
    "convex-test": "^0.0.44",
    "husky": "^9.1.7",
    "turbo": "^2.8.21",
    "typescript": "workspace:*",
    "vite": "^7.3.1",
    "vitest": "^4.1.9"
  },
  "engines": {
    "node": "24.x"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@ai-sdk/mcp": "^1.0.42",
    "@ai-sdk/openai": "3.0.63",
    "@ai-sdk/react": "3.0.184",
    "@convex-dev/better-auth": "^0.11.3",
    "@convex-dev/workflow": "0.4.4",
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "@ffprobe-installer/ffprobe": "^2.1.2",
    "@workspace/ai": "workspace:*",
    "ai": "6.0.182",
    "bash-tool": "^1.3.16",
    "better-auth": "1.5.5",
    "canvas-confetti": "^1.9.4",
    "convex": "^1.42.1",
    "pino": "^10.1.0",
    "streamdown": "^2.5.0",
    "zod": "^4.1.12"
  },
  "knip": {
    "ignore": [
      "apps/web/src/canvas-confetti.d.ts",
      "apps/web/src/electron.d.ts",
      "apps/web/src/vite-env.d.ts",
      "apps/web/tests/setup.ts"
    ]
  }
}
